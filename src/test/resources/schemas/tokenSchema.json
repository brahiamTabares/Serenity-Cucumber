{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "properties": {
    "emision": {
      "type": "string",
      "format": "date-time",
      "description": "Momento en que el token fue emitido"
    },
    "emisor": {
      "type": "string",
      "description": "Nombre de la entidad que emite el token"
    },
    "id": {
      "type": "string",
      "format": "uuid",
      "description": "Identificador único del token"
    },
    "propiedades": {
      "type": "object",
      "description": "Conjunto de propiedades adicionales que se almacenan en el token"
    },
    "roles": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Listado de roles del usuario"
    },
    "token": {
      "type": "string",
      "description": "Token de seguridad generado para el usuario"
    },
    "usuario": {
      "type": "string",
      "description": "Usuario para el que se generó el token"
    },
    "vigencia": {
      "type": "string",
      "format": "date-time",
      "description": "Momento en que el token perderá su validez"
    }
  },
  "required": ["emision", "emisor", "id", "roles", "token", "usuario", "vigencia"],
  "additionalProperties": false,
  "description": "Esquema para validar un objeto JSON que representa un token",
  "nullable": false,
  "readOnly": false,
  "writeOnly": false,
  "deprecated": false
}

